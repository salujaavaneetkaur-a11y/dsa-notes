class Solution{
    public boolean containsDuplicate(int[] nums){
        HashSet<Integers> seenNumbers = new HasSet<>();

        for(int num:nums){
            if(seenNumbers.contains(num))
            return true;
            seenNumbers.add(num);
        }
        return false;
    }
}